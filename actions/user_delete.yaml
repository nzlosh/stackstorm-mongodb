---
  name: "user_delete"
  runner_type: "python-script"
  description: "Delete an existing database user."
  enabled: true
  entry_point: "user_delete.py"
  parameters:
    profile_name:
      type: string
      description: "Mongodb profile to be used for the action."
      required: false
    username:
      type: string
      description: "Name of the user to be created."
      required: true
    db_name:
      type: string
      description: "The name of database to create the collection on."
      required: true
